const Monitor = (function() {
	const Monitor = function() {
		const empty = function() {};
		this.KEY_ENTER = empty;
		this.KEY_EXIT = empty;
		this.ENTER = empty;
		this.EXIT = empty;
		this.EVENT = empty;
		this.OPTIONS = empty;
		this.RESULTS = empty;
		this.DATA = empty;
		this.ASSERT = empty;
	};
	return Monitor;
})();
